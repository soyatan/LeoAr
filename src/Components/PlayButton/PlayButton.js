import React from 'react';
import {View, Text, TouchableOpacity} from 'react-native';
import styles from './styles';

const PlayButton = () => {
  return (
    <>
      <TouchableOpacity style={styles.TriangleShapeCSS}></TouchableOpacity>
    </>
  );
};

export default PlayButton;
